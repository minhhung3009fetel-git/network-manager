name: "Cấu hình SNMP (Chỉ đọc)"
description: "Kích hoạt SNMPv2c với community string chỉ đọc."
variables:
  - name: "community_string"
    prompt: "Nhập SNMP community string"
    default: "public"
commands:
  cisco_ios:
    - "snmp-server community {{ community_string }} RO"
